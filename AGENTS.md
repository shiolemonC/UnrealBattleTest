# BattleTest 项目协作规范

## 项目定位

BattleTest 是一个基于 Unreal Engine 5.5 的 C++ 第三人称 3D 动作游戏练习项目。

项目的长期目标是使用 Gameplay Ability System（GAS）构建一套接近《只狼》战斗体验的 Demo，逐步包含攻击、格挡、弹反、闪避、反击、处决及相关战斗状态与反馈。

当前项目同时承担学习用途。项目所有者有 Unity 开发经验，但此前没有 Unreal Engine 开发经验。因此，技术选择不仅要能完成当前功能，还应帮助建立正确、可迁移的 Unreal 开发认知。

## 协作方式

- 永远先确定方案，再开始施工。
- 在修改代码、配置或资产之前，先说明目标、设计边界、涉及文件或资产、替代方案、主要取舍和验证方式。
- 未经明确确认，不进入实现阶段。只读检查、问题定位和方案论证可以先进行。
- 如果新发现会显著改变已确认方案，应暂停施工，解释原因并重新确认。
- 不把当前需求擅自扩展为相邻系统，也不以“顺便优化”为由引入未经确认的架构改动。
- 对不可逆、批量、跨系统或可能影响已有资产的操作保持谨慎，并在执行前明确说明影响。
- 每轮工作结束时，说明完成内容、验证结果、仍存在的问题和建议的下一步。

## 讲解要求

- 默认使用中文沟通和编写面向项目所有者的说明。
- 解释 Unreal 特有概念时，可主动与 Unity 中的相近概念对照，但也要指出二者语义和生命周期上的差异。
- 第一次接触的编辑器配置、Animation Blueprint、状态机、GAS 等流程，需要给出较详细的操作路径和背后原因。
- 不只提供“点击哪里”，还要说明数据从哪里产生、由谁消费、何时更新，以及该设置为什么属于当前层级。
- 对已经解释并掌握的基础内容可以逐渐精简，避免长期重复教程式说明。
- 明确区分事实、推断、临时测试和最终方案；不要把尚未验证的猜测描述成结论。

## 架构原则

- C++ 是核心玩法、可复用系统、规则、状态和权威逻辑的主要实现层。
- Blueprint 主要用于资产配置、组合、派生、可视化调参和内容复用，不承载大段复杂或关键玩法逻辑。
- 一个功能的基础能力优先由 C++ 提供；不同角色、敌人或资产之间的内容差异可以通过 Blueprint 或数据资产配置。
- 不为了完全避免 Blueprint 而把所有资产引用和调参项硬编码进 C++；应保持代码职责与内容职责的合理边界。
- GAS 是长期战斗系统的核心框架。新增战斗功能时，应考虑其与 Ability、Attribute、Gameplay Effect、Gameplay Tag 和 Gameplay Cue 的长期关系，但避免在需求尚未明确时提前搭建大而全的系统。
- 优先采用可组合、可测试、低耦合的职责划分，避免角色类逐渐成为包含全部玩法逻辑的“上帝类”。
- Gameplay Tag、接口、组件和数据驱动方案应在确实能改善扩展性或表达力时使用，不为了形式而增加抽象层。
- 为未来单机演示之外的可预测性、网络语义和调试能力保留合理空间，但当前需求没有要求时，不擅自扩大到完整多人游戏实现。

## C++ 与 Blueprint 边界

适合放在 C++ 中的内容包括：

- 玩法规则和状态转换；
- 输入到玩法意图的处理；
- 角色、武器、投射物和战斗交互的基础能力；
- GAS 的基础集成和权威执行逻辑；
- 需要复用、测试、维护或网络同步的逻辑。

适合放在 Blueprint 或数据资产中的内容包括：

- Skeletal Mesh、Animation Blueprint、Montage、音效、特效等资产选择；
- 不同角色或敌人的内容组合与派生配置；
- 面向设计调试的数值和表现参数；
- 少量、清晰且不属于核心规则的装配逻辑。

当边界不明确时，先提出方案并说明取舍，不直接选择实现层。

## Unreal 资产原则

- 将 Marketplace、示例项目、模板和 Nodachi 等第三方内容视为外部来源资产。
- 原则上不直接修改外部来源资产；需要定制时，优先复制到项目自有目录，再使用明确的项目命名进行修改。
- 项目自有角色及其动画相关资产优先放在 `Content/Characters/Player` 或其清晰的子目录中；其他系统使用语义明确的项目目录。
- 避免产生多个难以区分的同名资产。引用资产时，应核对完整 Content Browser 路径，而不只核对显示名称。
- 修改 Skeleton、Socket、Control Rig、Animation Sequence 等共享资产前，先检查其引用范围和潜在连锁影响。
- 不通过文本或二进制方式直接编辑 `.uasset`、`.umap`；这类资产应通过 Unreal Editor 或可靠的 Unreal 自动化接口修改。
- 删除或移动 Unreal 资产时要考虑 Redirector，并在确认引用安全后处理。
- 资产配置属于实现的一部分，应和 C++ 一样经过方案确认、记录和验证。

## 动画与角色移动原则

- 始终明确区分角色在世界中的实际移动、动画中的视觉运动以及 IK 等运行时姿势修正。
- 调整动画前，先确认问题来自 Character Movement、状态机、Animation Sequence、Root Motion、Control Rig、Post Process，还是资产骨骼数据。
- 动画状态机负责持续状态的姿势选择；主动战斗动作如何播放，应根据可中断性、位移控制和 GAS 生命周期单独设计。
- IK 主要用于接触和姿势修正，不应被用来替代完整的跨步、攀爬、攻击或处决动作。
- 对动画包的骨骼、IK Bone、Socket 和 Root Motion 约定先进行验证，不假定同名 Manny 资产具有相同配置。

## 实现质量要求

- 遵循 Unreal Engine 的命名、生命周期、反射宏、对象所有权和组件模型。
- 保持头文件依赖最小化；可以前置声明时优先前置声明。
- Unreal 对象和组件引用应使用符合引擎垃圾回收和所有权规则的类型与标记。
- 对可在编辑器中配置的设计参数提供清晰的 Category、命名、Tooltip 或注释。
- 注释解释设计意图、约束和非显然原因，不重复代码表面行为。
- 不保留无用途的调试逻辑、临时资源引用或被遗忘的实验分支。
- 避免无关格式化和大范围机械改动；尊重工作区中已有且与当前任务无关的用户修改。

## 验证原则

- 验证强度应与改动风险匹配。
- C++ 改动至少应通过对应 Editor Target 的编译；影响运行时行为的功能还应说明 PIE 验证步骤。
- Blueprint、动画、输入和资产配置应在编辑器中编译、保存，并使用能够暴露问题的测试场景验证，而不只在理想平地或单一动画上观察。
- 修复问题时优先设计 A/B 对照和最小复现，一次只改变一个关键变量。
- 不以“编译成功”代替玩法验证，也不以“看起来正常一次”代替边界条件验证。
- 无法由自动化工具验证的编辑器操作，应给出明确、可复现的人工验证清单。

## Git 与项目卫生

- Unreal 二进制资产应由 Git LFS 管理；提交前检查新增大文件是否被正确跟踪。
- 不提交 `Binaries`、`DerivedDataCache`、`Intermediate`、`Saved` 等生成目录。
- 不覆盖、回滚或清理与当前任务无关的用户改动。
- 提交应按可理解的功能边界组织，避免将无关资产、实验文件和生成产物混入同一提交。
- 在执行提交、推送、历史改写、删除或大规模移动前，确认操作范围与用户授权。

## 决策优先级

发生冲突时，按以下顺序判断：

1. 用户本轮明确要求；
2. 已确认的功能方案和项目目标；
3. 本文件中的协作与架构原则；
4. Unreal Engine 官方推荐和现有项目约定；
5. 最小复杂度、最小风险且便于后续扩展的实现。

本文件记录长期协作方式和设计边界，不用于固定具体功能实现。随着项目目标或团队约定变化，可以在明确确认后更新。
